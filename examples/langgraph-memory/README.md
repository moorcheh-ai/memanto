# Memanto LangGraph V3 Integration Demo

This example demonstrates the use of `MemantoStore` as a `BaseStore` implementation for long-term semantic memory.

## Verification
Run the following command to verify cross-session persistence:
```bash
python examples/langgraph-memory/full_pipeline_demo.py
```
