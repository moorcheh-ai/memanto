MEMANTO_BASE_URL=http://127.0.0.1:8000
MEMANTO_AGENT_ID=langgraph-support-agent

# Dry-run mode uses a local JSON memory file so the example can be recorded
# without credentials. Set to 0 to use a running Memanto server.
MEMANTO_DRY_RUN=1
MEMANTO_DRY_RUN_STORE=.memanto-demo-store.json
