# Optional. When set, the demo uses real Memanto memory via Moorcheh.
# Without this key, the demo uses .local_research_memories.json so it can run
# in tests and local walkthroughs.
MOORCHEH_API_KEY=
