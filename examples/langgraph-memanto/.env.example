MOORCHEH_API_KEY=your_moorcheh_api_key_here

# Optional: set this to use a real LangChain chat model for the final response.
# Without it, the demo runs in deterministic offline mode so you can verify the
# memory flow without another provider key.
# OPENAI_API_KEY=your_openai_api_key_here
# LANGGRAPH_MEMANTO_MODEL=gpt-4o-mini
