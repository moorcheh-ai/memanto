# Notion → Memanto → OKF migration example
# Core (always required)
moorcheh-sdk>=1.3.5
pyyaml>=6.0

# Test runner
pytest>=7.0

# Optional: live Notion API fetch (only needed for --notion-live)
notion-client>=2.3.0

# Optional: LLM-as-judge for recall scoring
anthropic>=0.25.0
