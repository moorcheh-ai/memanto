# Exact-pinned deps for the ChatGPT/Claude migration demo.
#
# These are the transitive imports the adapter and OKF exporter reach
# (fastapi for path-safety validation, pydantic-settings for config, pyyaml
# for OKF frontmatter). They are pinned so every run resolves identically —
# the reproducibility contract of the showcase. Versions match a memanto
# checkout's locked environment.
#
# If you already cloned the full memanto repo and installed it with
# `pip install -e .`, you can skip this file and just run ./run_migration.sh.
fastapi==0.141.1
pydantic==2.13.4
pydantic-settings==2.15.0
PyYAML==6.0.3
