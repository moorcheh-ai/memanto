# n8n migration demo recording

This is the silent screen recording used as the live-proof artifact for the
n8n execution-history migration showcase. It shows the real source webhook
runs, the Memanto import, three grounded answers, and the portable OKF export.

| Property | Value |
| --- | --- |
| Container | MP4 |
| Video codec | H.264 / YUV420p |
| Resolution | 960 × 480 |
| Duration | 70.4704 seconds |
| Size | 851,019 bytes |
| SHA-256 | `8a852934c1d620b19e1046b5021daf0cc8840464148044c6007ffe84219d7c58` |

The recording is silent and does not display the API key. Local machine paths
are excluded from the capture. The polished HyperFrames composition remains a
separate preview; this committed artifact is the already-validated live
pipeline recording and does not claim an unrecorded result.
