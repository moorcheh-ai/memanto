# The SDK's SQLite file is regenerated by generate_session.py (and *.db is
# gitignored repo-wide). sample/source/session_snapshot.json is the committed,
# verbatim record of its contents.
sample/source/*.db
sample/source/*.db-wal
sample/source/*.db-shm
