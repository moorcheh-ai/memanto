# Requirements for CrewAI + Memanto Integration Example

# Core dependencies
crewai>=0.80.0
memanto>=0.1.0

# Additional dependencies (likely already installed with crewai)
pydantic>=2.0.0
python-dotenv>=1.0.0

# Optional: For LLM providers
# openai>=1.0.0  # If using OpenAI
# anthropic>=0.18.0  # If using Claude
# langchain-openai>=0.1.0  # For OpenAI integration with CrewAI
